# Broken: пароль в ConfigMap — виден всем через kubectl get cm -o yaml
# Пароли ДОЛЖНЫ быть в Secret, не в ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: db-config-bad
data:
  DATABASE_NAME: appdb
  DATABASE_USER: appuser
  DATABASE_PASSWORD: apppass    # ❌ пароль в ConfigMap! Виден в kubectl describe
  # Fix: перенести PASSWORD в Secret
  # apiVersion: v1
  # kind: Secret
  # data:
  #   password: YXBwcGFzcw==   # base64
