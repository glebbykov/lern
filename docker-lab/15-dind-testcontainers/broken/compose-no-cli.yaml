# Broken: socket mount есть, но docker CLI нет в образе
services:
  runner:
    image: alpine:3.20
    container_name: broken-no-cli
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: ["sh", "-c", "echo 'runner started' && sleep infinity"]
