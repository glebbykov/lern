# Изоляция процессов в Linux — путь к контейнеризации

Сквозной курс из 12 этапов. Цель — собрать у студента модель: что такое
контейнер на самом деле, поэтапно реализовав каждый его слой руками,
без Docker. К концу курса студент сам пишет «свой Docker» из ~150 строк
bash, используя только примитивы ядра.

## Карта маршрута

| # | Этап | Что добавляет | Что Docker делал бы за тебя |
|---|---|---|---|
| 00 | setup | проверка ядра, установка утилит | — |
| 01 | chroot | свой rootfs, изоляция ФС | распаковка image |
| 02 | namespaces | UTS/PID/MNT/NET/USER/IPC | `unshare` всего сразу |
| 03 | pivot-root | безопасная смена корня | то же что в `runc` |
| 04 | cgroups-v2 | лимиты CPU/RAM/IO/PIDs | `--memory`, `--cpus`, `--pids-limit` |
| 05 | capabilities | дробление root-прав | `--cap-drop`, `--cap-add` |
| 06 | seccomp | фильтрация системных вызовов | `--security-opt seccomp=...` |
| 07 | apparmor | мандатный контроль доступа | `--security-opt apparmor=...` |
| 08 | overlayfs | слоистая ФС, CoW, whiteout | `docker pull`, `docker commit` |
| 09 | networking | veth, bridge, NAT | сеть `bridge` (`docker0`) |
| 10 | rootfs+nspawn | сборка rootfs, systemd-nspawn | `FROM alpine`, `docker run` |
| 11 | capstone | свой контейнер из ~150 строк bash | весь `docker run` |

## Кому подойдёт

- Понимаешь Linux на уровне «ps, mount, ip a», но Docker — магия.
- Хочешь читать `runc`/`crun` исходники без шока.
- Готовишься к собеседованию, где спросят «что такое контейнер».

## Системные требования

- Ubuntu 22.04 / 24.04 LTS (в других дистрибутивах часть команд отличается).
- Ядро **>= 5.10** (для cgroups v2 unified, time namespace, дружественной seccomp).
- Реальные root-права (sudo). На контейнерных хостах часть лаб не пройдёт
  (вложенные namespaces, AppArmor требует загрузки в ядро).
- ~2 ГБ свободного места под rootfs alpine + debootstrap.

Проверка окружения и установка зависимостей — в [00-setup](./00-setup/).

## Как проходить

```bash
# проверить готовность хоста
sudo ./00-setup/check.sh

# установить всё что нужно (один раз)
sudo ./00-setup/install.sh

# каждый этап — это README.md (теория + шаги) + run.sh (демо) + check.sh
# (программная проверка). Делать по порядку:
cd 01-chroot && sudo ./run.sh && sudo ./check.sh

# или прогнать всё подряд
sudo ./run-all.sh
```

`run.sh` показывает шаги с пояснениями. `check.sh` — это автотест
успеха этапа (даёт `OK`/`FAIL`). Если хочешь читать вручную — открывай
`README.md` каждой папки.

## Связь с другими лабами в репозитории

- `process/labs/00-basics`, `01-lab` — базовая работа с процессами,
  fd, сигналами. **Пройди их сначала**, если эти темы непривычны.
- `docker-lab/07-security/hardening-lab` — следующий шаг: применение
  всех этих примитивов к продовому Docker Compose.

## Что в `legacy/`

Оригинальные 7 лабораторных, на основе которых построен курс. Сохранены
для совместимости и если хочется пройти исходную, более теоретическую
версию.

## Структура каждой папки

```
NN-stage/
├── README.md     теория + пошаговая практика
├── run.sh        демо-скрипт (показывает что и почему происходит)
├── check.sh      автотест (что-то пишет в /tmp, проверяет, чистит)
└── ANSWERS.md    разбор контрольных вопросов
```
