
# Решения к лабораторной работе: Циклы и условные выражения в Bash

## Задание 1: Использование циклов

### Скрипт для цикла `for`:
```bash
#!/bin/bash
# Вывод чисел от 1 до 10 через пробел
for i in {1..10}; do
    echo -n "$i "
done
echo
```

### Скрипт для цикла `while`:
```bash
#!/bin/bash
# Вывод чисел от 1 до 10 в столбик
i=1
while [ $i -le 10 ]; do
    echo $i
    ((i++))
done
```

### Скрипт для цикла `until`:
```bash
#!/bin/bash
# Вывод чисел от 10 до 1 через пробел
i=10
until [ $i -lt 1 ]; do
    echo -n "$i "
    ((i--))
done
echo
```

## Задание 2: Использование условных выражений
```bash
#!/bin/bash
# Проверка файла на существование и тип
if [ -e "$1" ]; then
    if [ -d "$1" ]; then
        echo "Это директория"
    elif [ -f "$1" ]; then
        echo "Это файл"
    else
        echo "Это что-то другое"
    fi
else
    echo "Файл не существует"
fi
```

## Задание 3: Использование `case`
```bash
#!/bin/bash
# Проверка дня недели
case $1 in
    "Понедельник")
        echo "Начинаем рабочую неделю!"
        ;;
    "Пятница")
        echo "Почти выходные!"
        ;;
    "Суббота" | "Воскресенье")
        echo "Ура, выходные!"
        ;;
    *)
        echo "Обычный рабочий день."
        ;;
esac
```

## Задание 4: Вложенные циклы и условные выражения
```bash
#!/bin/bash
# Таблица умножения от 1 до 5
for i in {1..5}; do
    for j in {1..5}; do
        echo "$i * $j = $((i * j))"
    done
done
```

## Дополнительные задания

### Задание 5: Факториал числа
```bash
#!/bin/bash
# Вычисление факториала числа
num=$1
fact=1
while [ $num -gt 1 ]; do
    fact=$((fact * num))
    ((num--))
done
echo "Результат: $fact"
```

### Задание 6: Числа Фибоначчи
```bash
#!/bin/bash
# Вывод первых N чисел Фибоначчи
n=$1
a=0
b=1
echo -n "$a $b "
for ((i=2; i<n; i++)); do
    fib=$((a + b))
    echo -n "$fib "
    a=$b
    b=$fib
done
echo
```

### Задание 7: Проверка на простое число
```bash
#!/bin/bash
# Проверка числа на простоту
num=$1
if [ $num -le 1 ]; then
    echo "$num не является простым числом."
    exit 0
fi

is_prime=1
for ((i=2; i*i<=$num; i++)); do
    if [ $((num % i)) -eq 0 ]; then
        is_prime=0
        break
    fi
done

if [ $is_prime -eq 1 ]; then
    echo "$num является простым числом."
else
    echo "$num не является простым числом."
fi
```
