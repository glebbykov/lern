# Конфиг для disk_usage_report.sh
# Этот файл автоматически загружается скриптом через source
# Значения здесь переопределяют дефолты, но флаги командной строки имеют приоритет

# Порог заполненности диска для предупреждения (в %)
THRESHOLD=75

# Файл для записи лога (оставить пустым для вывода только в stdout)
# LOGFILE="/var/log/disk_report.log"
